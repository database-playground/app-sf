<turbo-frame id="app-profile-password-update">
    <h3 class="mb-3">密碼更新</h3>

    {% if password_updated %}
        <div class="alert alert-success" role="alert">
            密碼更新完成 🎉
        </div>
    {% endif %}

    {{ form(password_change_form) }}
</turbo-frame>