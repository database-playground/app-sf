<table {{ attributes.defaults({ class: "table table-light table-hover" }) }}>
    <thead>
    <tr>
        <th scope="col">#</th>
        {% for cell in this.header %}
            <th scope="col">{{ cell }}</th>
        {% endfor %}
    </tr>
    </thead>
    <tbody>
    {% for column in this.rows %}
        <tr>
            <th scope="row">{{ loop.index }}</th>
            {% for cell in column %}
                <td>{{ cell }}</td>
            {% endfor %}
        </tr>
    {% endfor %}
    </tbody>

    {% include 'components/Challenge/ResultPresenterModule/Pagination.html.twig' %}
</table>